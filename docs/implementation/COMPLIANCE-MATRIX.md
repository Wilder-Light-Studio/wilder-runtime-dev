# Wilder Cosmos Runtime - Compliance Matrix

This matrix maps requirements to verification methods and test artifacts.
Status values:
- Planned: requirement mapped but test implementation may still be stubbed.
- In Progress: verification implementation is underway.
- Verified: verification exists and is runnable in current workflow.

| Requirement Area | Requirement Statement | Verification Method | Test Artifact | Status |
| --- | --- | --- | --- | --- |
| Core Architectural Principles | Pull-only participation, reversible entry/exit, and growth model are enforced. | Behavioral and negative tests; architecture review against push-mutation paths. | docs/implementation/REQUIREMENTS.md (Core Principle Use Cases), tests/core_principles_test.nim | Verified |
| Storage and Persistence Requirements | Reconciliation succeeds with any two layers or one full copy plus txlog. | Corruption, removal, and replay scenario tests. | tests/reconciliation_test.nim | Verified |
| Storage and Persistence Requirements | File-backed persistence uses deterministic layout, atomic replace semantics, and per-transaction txlog append. | FileBridge roundtrip, atomic-write simulation, txlog append idempotence, and replay tests. | tests/ch3_uat.nim, tests/reconciliation_test.nim | Verified |
| Storage and Persistence Requirements | Checksums/signatures are validated before acceptance. | Negative tests with malformed checksum/signature metadata. | tests/ch3_uat.nim, tests/reconciliation_test.nim, tests/serialization_test.nim | Verified |
| Storage and Persistence Requirements | Snapshot restore validates checksum/signature metadata before primary-layer replacement and halts on corruption. | Restore-path tests with valid and corrupt snapshots. | tests/ch3_uat.nim | Verified |
| Console Subsystem Requirements | Three-layer layout order is invariant. | Snapshot and deterministic rendering tests. | tests/console_status_test.nim | Verified |
| Console Subsystem Requirements | Detached command guardrails are enforced. | Command precondition tests for detached state. | tests/console_status_test.nim | Verified |
| Console Subsystem Requirements | Thin CLI entrypoint enforces required config, attach/detach neutrality, and watch-stop behavior. | CLI launch tests, missing-config exit tests, and attach/watch/detach behavior checks. | tests/console_status_test.nim, src/console_main.nim | Verified |
| Ontology Requirements | World changes flow only through Occurrences; five primitives enforced (Thing/World, Occurrence, Wave, Perception, RECORD). | Type/schema and behavior tests. | tests/ontology_test.nim, tests/ch4_ontology_test.nim | Verified |
| Runtime Start Coordinator Requirements | Runtime entrypoint CLI enforces launch contract, mode branching, watch constraints, and structured failure semantics. | Coordinator CLI argument/branching tests and startup halt assertions. | tests/coordinator_test.nim, src/cosmos_main.nim | Verified |
| Runtime Lifecycle Requirements | Startup sequence is deterministic and halts on irreconcilable failure. | Integration tests with ordered lifecycle assertions. | tests/lifecycle_test.nim, tests/integration_test.nim | Verified |
| Runtime Lifecycle Requirements | Module loading is blocked before reconciliation; ingress is blocked before prefilter activation; startup halts include recovery guidance. | Gate enforcement tests and structured-error assertions. | tests/lifecycle_test.nim, tests/integration_test.nim | Verified |
| Host Observability Requirements | Structured host events are emitted for startup, reconciliation, migration, prefilter activation, and shutdown without leaking raw payloads. | Event-count assertions, event-kind assertions, and safe-log inspection tests. | tests/integration_test.nim, src/runtime/observability.nim | Verified |
| Interrogative Manifest Requirements | Thing/World validates on WHO and WHY minimum; optional interrogatives (WHAT/WHERE/WHEN/HOW/NEEDS/WANTS/PROVIDES) validate only when present. | Validation tests for minimum contract acceptance and optional-field validation behavior. | tests/interrogative_test.nim, tests/ontology_test.nim | Verified |
| Capability Discovery Requirements (Phase XA) | DRY wants/provides boundary rules, deterministic capability graph construction, fatal resolution failure classes, and CLI visibility (`cosmos capabilities`, `cosmos concept resolve`) are implemented. | Resolver edge-case tests, deterministic-order tests, startup-gate integration tests, and coordinator CLI command tests. | docs/implementation/REQUIREMENTS.md, docs/implementation/SPECIFICATION-NIM.md, tests/capabilities_test.nim, tests/coordinator_test.nim, tests/lifecycle_test.nim, tests/integration_test.nim | Verified |
| Semantic Scanner Requirements (Phase XB) | Deterministic scanner infers needs/wants/provides/conflicts/before/after from source files, emits Thing objects, and exposes `cosmos scan` and `cosmos capability conflicts` commands. | Scanner inference tests, deterministic rerun checks, and coordinator CLI command tests. | docs/implementation/REQUIREMENTS.md, docs/implementation/SPECIFICATION-NIM.md, tests/semantic_scanner_test.nim, tests/coordinator_test.nim, src/runtime/scanner.nim | Verified |
| Runtime Messaging Strategy Requirements (Phase XC) | Coordinator IPC request/response/event contracts with localhost endpoint validation, required pause/resume/step/snapshot/inspect methods, subscriptions, and secondary notification stream formatting are implemented. | Dedicated coordinator IPC unit tests and coordinator CLI command contract tests. | docs/implementation/REQUIREMENTS.md, docs/implementation/SPECIFICATION-NIM.md, src/runtime/coordinator_ipc.nim, tests/coordinator_ipc_test.nim, tests/coordinator_test.nim | Verified |
| Encrypted RECORD Requirements (Phase XD) | Deterministic encrypted RECORD entry primitives and metadata-only chain reconciliation checks are implemented without payload decrypt during validation. | Encrypted RECORD deterministic/chain tests and metadata comparison checks. | docs/implementation/REQUIREMENTS.md, docs/implementation/SPECIFICATION-NIM.md, src/runtime/encrypted_record.nim, tests/encrypted_record_test.nim | Verified |
| Module Contract Requirements | Cosmos-native module contracts are code-defined and external-process wrappers accept handwritten manifests as authoritative. | Registry tests for code-defined contracts, manifest generation, and external wrapper registration. | tests/module_test.nim, tests/ontology_test.nim | Verified |
| Status Requirements | Status invariants run at load, mutation, and reconciliation. | Invariant enforcement tests. | tests/status_memory_test.nim | Verified |
| Memory Requirements | Memory caps are bounded, introspectable, and errors are structured. | Boundary and overflow tests. | tests/status_memory_test.nim | Verified |
| Delegation and Specialization Requirements | Delegation is occurrence-only, deterministic, visible, and reversible. | Async flow tests and Console introspection checks. | tests/delegation_test.nim | Verified |
| Scheduler Requirements | Deterministic ordering and bounded execution are enforced. | Replay determinism and failure suspension tests. | tests/scheduler_test.nim | Verified |
| Security Requirements | No cross-instance leakage and no hidden channels. | Permission boundary and cross-instance isolation tests. | tests/security_boundary_test.nim | Verified |
| Documentation Requirements | Terms are defined, diagrams exist, and command examples are explicit. | Static compliance gate checks in build workflow. | scripts/check_requirements.ps1 | Verified |
| Documentation Requirements | Validation firewall terminology is applied in normative docs, plans, and user-facing test/comment text, and the Chapter 2C test artifact names are aligned with the updated vocabulary. | Static terminology audit and plan review. | docs/implementation/REQUIREMENTS.md, docs/implementation/SPECIFICATION-NIM.md, docs/implementation/PLAN.md, docs/PLAN.md, tests/ch2_uat.nim, tests/validation_firewall_test.nim | Verified |
| Documentation Requirements | Every chapter includes `MODULE-FLOWCHARTS.md` and touched modules are represented in flowchart references. | Static chapter-artifact checks and documentation review. | docs/implementation/Chapter1/MODULE-FLOWCHARTS.md, docs/implementation/Chapter2/MODULE-FLOWCHARTS.md, docs/implementation/Chapter3/MODULE-FLOWCHARTS.md, docs/implementation/Chapter4/MODULE-FLOWCHARTS.md, docs/implementation/Chapter5/MODULE-FLOWCHARTS.md, docs/implementation/Chapter6/MODULE-FLOWCHARTS.md | Verified |
| Documentation Requirements | Module headers include Summary/Simile/Memory note/Flow and each proc has a Flow comment. | Static comment-coverage checks across all `.nim` modules. | scripts/check_requirements.ps1 | Verified |
| Documentation Requirements | Nim module headers include identity lines (`Wilder Cosmos`, `Module name`, `Module Path`) and are generated from canonical templates when creating new modules. | Static checks for required header identity lines and artifact presence. | scripts/check_requirements.ps1, templates/headers/*.tpl, scripts/new_nim_module.ps1 | Verified |
| Release and Packaging Requirements | Compliance checks run in normal dev verification flow. | Task execution validation in Nimble tasks. | wilder_cosmos_runtime.nimble (`compliance`, `verify`) | Verified |
| Release and Packaging Requirements | Inactive pre-release CI workflow exists and is not auto-triggered. | Static workflow file presence and inactive-condition checks. | .github/workflows/pre_release_verify.yml, scripts/check_requirements.ps1 | Verified |
| Release and Packaging Requirements | Phase 19A cross-platform target matrix, staged release flow, checksum automation, and installer contract checks are present and runnable in an inactive scaffold workflow. | Static workflow + script validation and manual dispatch verification. | .github/workflows/release_artifacts.yml, scripts/generate_release_manifest.ps1, scripts/generate_artifact_checksums.ps1, scripts/test_installer_contract.ps1 | Verified |
| Runtime Configuration Requirements | Config loads from Cue-exported source; invalid combinations rejected at startup. | Config loading and validation tests for debug/production modes and invalid fields. | tests/config_test.nim | Verified |
| Runtime Configuration Requirements | Cue-exported config is validated by script before runtime load. | Script execution tests with valid and invalid exports. | scripts/validate_config.ps1, scripts/test_validate_config_script.ps1 | Verified |
| Runtime Configuration Requirements | Env and CLI overrides apply in documented precedence order and use the same validation rules as file values. | Override precedence tests and invalid-override rejection tests. | tests/config_test.nim | Verified |
| Runtime Configuration Requirements | No subsystem reads raw config after startup; config injected once. | Architecture review and integration tests asserting single load point. | tests/config_test.nim, tests/integration_test.nim | Verified |
| Messaging System Requirements | MessageEnvelope structure and forward-compatible Protobuf schema are correct. | Protobuf schema review and round-trip tests. | proto/messaging.proto, tests/messaging_test.nim, tests/serialization_test.nim | Verified |
| Messaging System Requirements | Debug mode logs all envelopes; production mode does not. | Mode-conditional logging tests. | tests/messaging_test.nim | Verified |
| Serialization Transport Requirements | JSON and Protobuf serializers round-trip MessageEnvelope without loss. | Round-trip unit tests for both serializers. | tests/serialization_test.nim | Verified |
| Serialization Transport Requirements | Correct serializer selected from config; filesystem bridge always uses JSON. | Config-driven selection tests and filesystem bridge tests. | tests/serialization_test.nim | Verified |
| Data Handling and Validation Requirements | All public procs validate inputs at the boundary with fail-fast behavior. | Input validation unit tests for all public procs. | tests/validation_test.nim, tests/api_tests.nim | Verified |
| Data Handling and Validation Requirements | Validation uses short-circuit evaluation, compile-time checks, and reusable helpers. | Architecture review and unit tests for validation helper functions. | src/runtime/validation.nim, tests/validation_test.nim | Verified |
| Data Handling and Validation Requirements | Errors include descriptive messages; sensitive data never logged. | Error handling tests; log inspection tests. | tests/validation_test.nim, tests/integration_test.nim | Verified |
| Data Handling and Validation Requirements | Persisted records include SHA256 checksums and validate on load/mutation/deserialization. | Checksum validation and corruption detection tests. | tests/validation_checksum_test.nim, tests/serialization_test.nim, tests/reconciliation_test.nim | Verified |
| Data Handling and Validation Requirements | Validation aligns with Confidentiality and Opacity principle; sensitive data protected. | Security boundary tests for data leakage; determinism tests for validation. | tests/security_boundary_test.nim, tests/validation_test.nim | Verified |
| Validating Prefilter Requirements | No proc/function receives unvalidated data; all inbound payloads are structurally validated before dispatch or recording. | Boundary and dispatch-gate tests with invalid payload injection attempts. | tests/validation_firewall_test.nim | Verified |
| Validating Prefilter Requirements | Signature-keyed rule lookup is O(1) and uses pre-resolved contracts with no dynamic schema parsing on hot path. | Performance microbenchmarks and architecture conformance checks. | tests/validation_firewall_perf_test.nim | Verified |
| Validating Prefilter Requirements | Prefilter table is generated from canonical signature/schema sources; no hand-maintained duplicated prefilter logic is used. | Build-pipeline generation checks and artifact/source digest conformance tests. | tests/validation_table_generation_test.nim | Verified |
| Validating Prefilter Requirements | Prefilter failures are emitted as safe, deterministic failure Occurrences that never leak raw invalid payload data to user code. | Failure-occurrence schema tests and negative dispatch tests. | tests/validation_failure_occurrence_test.nim | Verified |
| Validating Prefilter Requirements | Validation masks are precomputed at build time for every signature; payload masks are computed at runtime without allocation. | Mask generation correctness tests; zero-allocation payload mask benchmarks. | tests/validation_firewall_test.nim, tests/validation_firewall_perf_test.nim | Verified |
| Validating Prefilter Requirements | Hot-path structural validation uses mask-based boolean conjunction, constant-time with respect to mask width. | Mask conjunction correctness and timing tests. | tests/validation_firewall_test.nim, tests/validation_firewall_perf_test.nim | Verified |

## Maintenance Rule

When a requirement changes in docs/implementation/REQUIREMENTS.md, update this matrix in the same change set.

---
*&copy; 2026 Wilder. All rights reserved.*\
*Contact: teamwilder@wildercode.org*\
*Licensed under the Wilder Foundation License 1.0.*\
*See LICENSE for details.*